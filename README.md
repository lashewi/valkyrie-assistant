# Valkyrie Speech Assistant

Simple Python app that uses speech recognition, text-to-speech and Google text-to-speech API

### Dependencies

```
pip install speechrecognition
pip install pyaudio
pip install gTTS
pip install playsound
pip install PyObjC
```

